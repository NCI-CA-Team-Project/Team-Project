// handles the accounts creatation and sends user data to backend 

export default function Register() {
  return (
    <div style={{ padding: 20 }}>
      <h1>Register</h1>
      <p>Placeholder page (wiring backend next).</p>
    </div>
  );
} //same logic as login not completed needs form and backend connection